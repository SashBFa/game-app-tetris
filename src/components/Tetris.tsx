interface tetrisProps {
  rows: number;
  colums: number;
  setGameOver: boolean;
}

const Tetris = ({ rows, colums, setGameOver }: tetrisProps) => {
  return (
    <div>
      <h1>Hey</h1>
    </div>
  );
};

export default Tetris;
